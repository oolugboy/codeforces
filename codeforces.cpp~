#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int getMax(long, long int m)
{
    int start = 1;
    int prev = 0;
    while(pow(start, 3)  <= m)
    {
        prev = start;
        start++;
    }
    return prev;
}

int 
int main()
{
    long long int m = 0;
    scanf("%lld",m);
    
    
    return 0;
}
